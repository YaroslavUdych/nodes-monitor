services:
   backend:
      build:
         context: .
         dockerfile: docker/backend.Dockerfile
      ports:
         - '3000:3000'

   frontend:
      build:
         context: .
         dockerfile: docker/frontend.Dockerfile
      ports:
         - '5173:80'
      depends_on:
         - backend
